# 开发/测试依赖（本地/CI 装）
# 与 requirements.txt 互补；本地开发：pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
ruff>=0.5
mypy>=1.10
pre-commit>=3.7
git-filter-repo>=2.45

# 桌面打包（PyInstaller + PyWebView 嵌 Chromium 窗）
pywebview>=4.4
pyinstaller>=6.0

# 浏览器冒烟（前端 e2e：无 playwright 时 test_e2e_smoke 自动 skip）
playwright>=1.40

